JWT SETUP

1>  make utils folder in which password.go
    >  make hashpassword func
    >  make comaprepassowrd func
2> make createUser in books.go
3> make handlers registerHanlder

4> Create Login
     >make types folder in which types.go
     